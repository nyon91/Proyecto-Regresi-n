#!/bin/bash

if [ "pruebas pasadas" ]; then
        cd /home/juan/Documentos/cron
        git add script
        git commit -m "committing any new configuration changes!"
        git push origin master
        echo ""
        echo "git repo updated"
else
        echo "Fallo las pruebas"
fi
